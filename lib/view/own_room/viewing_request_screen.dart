import 'package:flutter/material.dart';

class ViewingRequestScreen extends StatelessWidget {
  const ViewingRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
